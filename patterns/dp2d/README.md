# 2D Dynamic Programming

Use 2D DP when state depends on row and column or two dimensions.

Unique Paths:

```text
dp[r][c] = ways from top + ways from left
```

Code:

- [unique_paths.go](unique_paths.go)
