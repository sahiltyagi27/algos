# Bit Manipulation

Common identities:

```text
x ^ x = 0
x ^ 0 = x
n & (n-1) clears the lowest set bit
```

Good for:

```text
single number
power of two
count bits
subsets with bitmask
```

Code:

- [bit_basics.go](bit_basics.go)
